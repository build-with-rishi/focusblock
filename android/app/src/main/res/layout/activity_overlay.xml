<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overlay_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/overlay_background"
    android:keepScreenOn="true">

    <!--
        Invisible input that captures the challenge typing. 1dp and fully
        transparent, but focusable so both the soft keyboard and hardware
        keyboards route text into it.
    -->
    <EditText
        android:id="@+id/hidden_input"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:alpha="0"
        android:background="@null"
        android:importantForAutofill="no"
        android:imeOptions="flagNoExtractUi|flagNoFullscreen"
        android:inputType="textVisiblePassword|textNoSuggestions"
        android:maxLines="1"
        android:textColor="@color/overlay_background"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingEnd="32dp">

        <TextView
            android:id="@+id/event_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@color/overlay_title"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- Time-to-event: the dominant element -->
        <TextView
            android:id="@+id/time_to_event"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:textColor="@color/overlay_time"
            android:textSize="64sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/event_meta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:textColor="@color/overlay_meta"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/quote"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="40dp"
            android:gravity="center"
            android:maxWidth="480dp"
            android:textColor="@color/overlay_quote"
            android:textSize="18sp"
            android:textStyle="italic" />

        <TextView
            android:id="@+id/challenge_word"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="48dp"
            android:fontFamily="monospace"
            android:gravity="center"
            android:letterSpacing="0.6"
            android:textColor="@color/overlay_letter_pending"
            android:textSize="30sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/dismiss_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:gravity="center"
            android:text="@string/overlay_dismiss_hint"
            android:textColor="@color/overlay_hint"
            android:textSize="12sp" />

    </LinearLayout>

</FrameLayout>
